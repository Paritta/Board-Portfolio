<!DOCTYPE html>
<html lang="en">

<head>
    <title>Bloggggggg</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!--<link rel="stylesheet" type="text/css" href="style.css">-->
    <!--<link href="css/style.css" rel="stylesheet">-->
    <!-- 합쳐지고 최소화된 최신 CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <link href='https://fonts.googleapis.com/css?family=Calligraffitti' rel='stylesheet'>
    <link href='https://fonts.googleapis.com/css?family=Basic' rel='stylesheet'>
    <link href='https://fonts.googleapis.com/css?family=Alegreya' rel='stylesheet'>
    <style>
        .page {
            display: flex;
            justify-content: center;
        }

        .button {
            margin-left: 10px;
        }

        .btn-group {
            display: flex;
            justify-content: flex-end;
            align-items: center;
        }

        .btn {
            margin: 2rem;
        }

        body {
            padding: 3rem;
            /* border: 7px dotted teal; */
        }

        .container {
            /* border: 7px dotted thistle; */
            width: 85%;
        }

        #header {
            margin-top: 20rem;
            margin-bottom: 15rem;
            text-align: center;
            font-family: 'Calligraffitti';
            font-size: 70px;
        }

        .boards {
            margin: 6rem;
            font-size: 25px;
        }

        .title {
            color:rgb(38%, 68.1%, 99.5%);
            font-family: 'Calligraffitti';
            font-size: 50px;
        }

        .description {
            font-family: 'Basic';
            font-size: 30px;
        }

        footer {
            /* margin-top: 1rem; */
            font-family: 'Alegreya';
            font-size: 15px;
            color:rgb(53.7%, 61.6%, 69.8%);
        }
        
        .link {
            font-family: 'Calligraffitti';
            font-size: 25px;
            margin-top: 2rem;
            margin-right: 4rem;
            text-align: right;
        }

        .username {
            font-family: 'Calligraffitti';
            font-size: 25px;
            margin-right: 4rem;
            text-align: right;
        }

    </style>
</head>

<body>
    <div class="container">
        <div class="username">
            <span>Welcome, <%= username %></span>
        </div>
        <div id="header">
            <p>Bloggggggg</p>
        </div>
        <div class="btn-group">
            <a class="btn btn-default btn-info btn-lg" href="/api/post/add">글쓰기</a>
        </div>
        <div class="boards">
            <% for (var i = 0; i < boards.length; i++) { %>
                <div class="title">
                    <%= boards[i].title %>
                </div>
                </br>
                <% description = boards[i].description.slice(0, 300) %>
                    <div class="description">
                        <%= description %>
                    </div>
                    <div class="link">  
                        <a href='/api/post/detail/<%= boards[i]._id %>'>
                            Read More
                        </a>
                    </div>
                    <footer>
                        <span>CreatedAt: <%= boards[i].createdAt %></span></br>
                        <span>Author: <%= boards[i].author%></span>
                    </footer>
                    </br>
                    <% } %>
        </div>
    </div>
    <!-- 부가적인 테마 -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap-theme.min.css">

    <!-- 합쳐지고 최소화된 최신 자바스크립트 -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>
</body>

</html>